<h2>投交简历到 <%= @job.title %></h2>
<hr />
<div class="col-md-4 col-md-offset-4">
  <%= simple_form_for [@job, @resume] do |f| %>
    <%= f.input :content %>
    <%= f.input :attachment %>
    
    <%= f.submit "送出" %>
  <% end %>
</div>
